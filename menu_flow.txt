Main Menu
(General menu navigation: Button 1: Down, Button 2: Up, Button 3: Select)
├── Survey mode --- Start/Stop Survey --- (blank) --- Menu (back to Main Menu)
├── Drive mode --- Start/Stop driving --- (blank) --- Menu (back to Main Menu)
├── Cosmic mode --- Start/Stop flight --- (blank) --- Menu (back to Main Menu)
├── Fixed mode --- (blank) --- (blank) --- Menu (back to Main Menu)
├── Satellite view
│   (When on Satellite View screen: Options (re-opens Satellite View menu) --- Reconnect --- Menu (back to Main Menu))
│   ├── Satellites (View constellation map)
│   ├── Set default (Configure GPS module to optimise use of satellites)
│   ├── Set Americas (Configure GPS module to optimise use of satellites)
│   ├── Set Europe/Africa (Configure GPS module to optimise use of satellites)
│   ├── Set Asia/Oceania (Configure GPS module to optimise use of satellites)
│   ├── Cold start (Action: Performs GPS cold start)
│   └── Factory reset (Action: Performs GPS factory reset)
├── Log viewer
│   (When on Main Log Viewer: Down --- Select (to enter log category or detail) --- Menu (back to Main Menu))
│   (When on Log List View: Down --- Select (to enter detail) --- Menu (back to Main Menu))
│   (When on Log Detail View: Back --- Upload --- Menu (back to Main Menu))
│   ├── Drive logs
│   ├── Survey logs
│   └── Journal logs
├── Settings
│   (When on a specific Settings detail page, e.g., 'View settings', 'Start Access Point')
│       --- Options (re-opens Settings menu list)
│       --- (Action varies by page, e.g., 'More' on 'View settings', otherwise often blank)
│       --- Menu (back to Main Menu)
│   ├── View settings
│   ├── Start Access Point
│   ├── Start on local
│   ├── Load from SD (Action: Loads settings from SD)
│   ├── Save to SD (Action: Saves current settings to SD)
│   ├── Wipe SD Card --- Yes --- (blank) --- No
│   ├── Reset dose (Action: Resets accumulated dose and restarts device)
│   ├── Factory reset --- Yes --- (blank) --- No
│   └── Back to main menu (Action: Returns to Main Menu)
├── About Zen --- Toggle QR/Info --- (blank) --- Menu (back to Main Menu)
└── Debug info --- (blank) --- (blank) --- Menu (back to Main Menu) 